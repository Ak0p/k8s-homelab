---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: syncthing
  namespace: syncthing
spec:
  interval: 5m
  chart:
    spec:
      chart: syncthing
      version: 23.0.1
      sourceRef:
        kind: HelmRepository
        name: syncthing
        namespace: syncthing
  values:
