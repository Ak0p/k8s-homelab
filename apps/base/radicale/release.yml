---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radicale
  namespace: radicale
spec:
  interval: 5m
  chart:
    spec:
      chart: radicale
      version: 8.17.2
      sourceRef:
        kind: HelmRepository
        name: radicale
        namespace: radicale
  values:
    radicale:
      auth:
        htpasswd_encryption: bcrypt
        users:
          - username: andrei
            password: $2y$05$PEI4HJmksx.WxBn4gOWC2u8Lx/bBuIlm//1KA8vivmQt5cutm3pbm
    securityContext:
      pod:
        fsGroupChangePolicy: "Always"
    persistence:
      data:
        enabled: true
        type: pvc
        retain: true
        accessModes: ReadWriteMany
        size: 2Gi
        storageClass: host-zfs-files
