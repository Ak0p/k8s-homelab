apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftpgo
  namespace: sftpgo
spec:
  storageClassName: host-zfs-files
  resources:
    requests:
      storage: 500Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
