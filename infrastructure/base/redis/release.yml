---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: redis
spec:
  interval: 5m
  chart:
    spec:
      chart: chart-name
      version: chart-version
      sourceRef:
        kind: HelmRepository
        name: redis
        namespace: redis